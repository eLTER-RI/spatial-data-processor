Spatial data (see shapefiles) is precomputed with geopandas for performance.

Geotiff is the only file format tested, although theoretically anything supported by [rasterio](https://rasterio.readthedocs.io/en/stable/) should work.
